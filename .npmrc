# Platform configuration for cross-platform compatibility
platform=linux
arch=x64

# Ensure we don't install optional dependencies that might cause platform issues
optional=false

# Use the latest npm for better platform handling
engine-strict=true

# Cache configuration for CI
cache=.npm
prefer-offline=true
